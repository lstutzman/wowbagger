# Handoff — wowbagger, project-wide (2026-08-07)

**Worktree:** `/Users/leestutzman/Documents/GitHub/wowbagger/.claude/worktrees/compressed-skipping-dongarra`
**Branch:** `worktree-compressed-skipping-dongarra` (this repo has no `staging`; `main` is default)
**Status:** clean tree; work through `eac8954` is on `origin/main`; later commits unpushed

> **Next agent:** read this file, then run the queue command below. Items are now
> numbered — say "item 30", not the ULID. The top of the queue is dogfood
> friction, and it is there deliberately.

---

## Where things stand

**Shipped today and on `origin/main`:**

- The Claude Code adapter, Plan 1 of 3: a shared engine in `src/adapter/`, a
  runnable package in `adapters/claude-code/`, and a conformance runner. **79 of
  183 assertions evidenced**, run status `fail`. That is correct — Plans 2 and 3
  evidence the rest, and §10's status table still reads `Unverified` for every
  platform. Detail in `docs/handoffs/2026-08-06-adapter-plan-1.md`.
- An installable plugin, self-hosted from this repo's own marketplace.
- **532 tests**, green on Node 26 and Node 20 (304 at the start of the day).

**Shipped today and NOT yet pushed** — four commits:

- `9af41f6`, `b0a68e4` — eight dogfood findings filed as ledger items.
- `b0ee411` — **priority restored** to the ready ordering.
- `eac8954` — **`number`**, a short integer handle on every item.

Plus uncommitted work described under "Do this first".

## Authoritative facts (do not re-derive)

- **Canonical test command:** `TMPDIR=/tmp node --test test/*.test.js`. The short
  `TMPDIR` is required — macOS caps `sockaddr_un.sun_path` at 104 bytes. Bare
  `node --test` also globs a non-test file and reports a phantom failure.
- **Node 20 lives at `/opt/homebrew/opt/node@20/bin/node`** — a Homebrew keg, so
  nvm/fnm/volta probes find nothing. Both runtimes must pass.
- **Never `git stash` here.** Worktrees share one stash stack.
- **`src/` must never import from `test/` or `spec/`.** `spec/adapter-reference.js`
  is an independent oracle that `src/adapter/` deliberately re-implements; the
  same arrangement exists between `src/claim-request.js` and
  `test/work-claim-reference.js`. Collapsing either pair makes its conformance
  tests prove nothing.
- **Claims are advisory and enforce nothing.** `mode: "advisory"`,
  `safe_exclusive_dispatch: false`. Nothing may change that without fencing.
- **The engine is deliberately stricter than the oracle on a non-string
  `request_id`.** That is correct per §3.3. Do not "reconcile" it backwards.
- **`ready --json` is byte-compared by the adapter conformance vectors.** Do not
  add fields to it. Human display belongs on a human surface.

## Read the queue

```sh
./bin/wowbagger.js validate --ledger ledger --json
./bin/wowbagger.js ready --ledger ledger --as-of <today> --json
```

`ready --json` returns bare ULIDs — the human view is item 28's remaining work.
Until it exists, read `number:`, `priority:` and `title:` from the item files.

Current top of queue, all dogfood friction:

| # | pri | Title |
|---:|---:|---|
| 30 | 1 | Give the ledger a way to express priority *(restored; see below)* |
| 26 | 10 | Tell the caller that a created item lands in triage |
| 29 | 10 | Make inspect consistent about which fields are promoted onto item |
| 32 | 10 | Decide whether ownership returns, after priority did |
| 33 | 10 | Let the CLI change an item's frontmatter, not just its status |
| 27 | 20 | Remove the ULID generator as a prerequisite for first use |
| 28 | 20 | Give every item a short human-readable handle *(field shipped; display open)* |
| 31 | 20 | Release-note the claim request `__proto__` behaviour change |

Below priority 20 sit the planned items (adapters, plugin, dogfood, fenced
claims, core gaps) in creation order.

---

## Do this first

**1. Commit and push the uncommitted work.** The tree currently holds: rewritten
bodies for items 28 and 30 (both described shipped work as still open), three
newly filed items (31, 32, 33), and their numbers. Validate, run both runtimes,
commit, push.

**2. Item 30 is mostly done.** Priority is restored and validated. What remains
is recording *why it was removed*, or recording that nobody knows.

## The priority story — read this before touching the ledger

Priority was not missing by design. It was **deleted**:

- `3f1a5e5` (4 Aug, 14:17) — founding direction: *"YAML metadata for lifecycle,
  **priority**, dependencies, and **ownership**"*.
- `73245c1` — priority **fully specified**: a non-negative integer supplied by a
  consumer policy, lower values first, core never calculates it, with a
  four-step ready ordering and worked fixtures at 1/5/10/20.
- `1058b8c` (4 Aug, 18:35) — *"docs: tighten standalone ledger invariants"*
  removed it from the README, SPEC, ADR-0001, the standalone plan, and every
  ready-selection fixture. **No ADR. No mention in the commit message.**

A consumer hit the hole three days later, and it had buried every dogfood
finding at the bottom of the queue.

`b0ee411` restores the original four-step rule verbatim and adds validation the
original never had — `priority: high` used to validate clean, so a restored
ordering would have silently ignored it.

**Ownership was deleted in the same commit and has not been examined.** That is
item 32.

**The lesson worth keeping:** a contract field was removed inside a
documentation-tidying commit and nothing caught it for three days. The ledger's
`decisions:` field exists precisely to prevent that. Wowbagger would have caught
this about itself if the removal had gone through its own ledger.

## Known-and-accepted

- `number` is not identity. Publication, references and filenames use the ULID.
  Two worktrees can allocate the same number; that is a `duplicate-number`
  validation error resolved at merge, not an identity collision.
- Changing any frontmatter field requires hand-editing Markdown — item 33. This
  bypasses validation, the per-ID lock, and compare-and-swap. `CONTRIBUTING.md`
  sanctions it as a reviewable Git change, but it is a real gap and it makes the
  plugin skill's own instruction impossible to follow.
- The plugin's `/plugin marketplace add lstutzman/wowbagger` flow is
  **unverified**. Nobody has installed it. First install is item 23's job and may
  fail at step one.
- Adapter Plans 2 and 3 carry-forwards are in
  `docs/handoffs/2026-08-06-adapter-plan-1.md`, including the deferred minors and
  the one authorship caveat found by the whole-branch review.

## Blockers

- **Fenced work claims (item 17) carry an unresolved design question**, not just
  unwritten code. The contract requires one transactional coordinator serializing
  the claim decision, clock floor, every write path, and ledger publication. The
  ledger is Markdown in Git, so either the ledger bytes move inside that
  coordinator — challenging the "plain Markdown and Git" thesis — or the
  definition of fenced changes. Settle with Lee before implementing. Full
  statement in `ledger/2026-08-06-fenced-work-claims-coordinator.md`.
- **The full PropertyCompass backlog migration is unauthorized** and stays
  `triage` (item 2). Gated on fencing: PropertyCompass writes from roughly twenty
  concurrent worktrees and advisory claims enforce nothing.

## Housekeeping

Four stale branches remain, all merged, with no worktrees:
`feature/mutation-contract`, `feature/mutation-runtime`,
`feature/read-only-kernel`, `feature/standalone-core-contract`, plus
`feature/harness-neutral-adapter-contract` and `feature/work-claim-contract`.
Deleting them was never authorised — ask first.

The main checkout at `/Users/leestutzman/Documents/GitHub/wowbagger` is behind
`origin/main` and needs a pull.

## Prompt for next session

```
Context: continuing wowbagger from 2026-08-07. Work happens in the wowbagger
repository only — not PropertyCompass2.

Read these first:
1. docs/handoffs/2026-08-07-project.md
2. docs/handoffs/2026-08-06-adapter-plan-1.md if touching the adapter

First action: the working tree holds uncommitted ledger work — rewritten bodies
for items 28 and 30, and three new items 31-33. Validate, run both runtimes,
commit, and push. Four earlier commits are also unpushed.

Items are numbered. Refer to them as "item 30", never by ULID.

Before any test run: TMPDIR=/tmp node --test test/*.test.js, and verify on both
Node 26 (`node`) and Node 20 (`/opt/homebrew/opt/node@20/bin/node`).
Never git stash in this repository.

The queue is priority-ordered and the top eight are dogfood friction. Start
there unless Lee redirects.
```
