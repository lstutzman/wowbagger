# Handoff — wowbagger, project-wide (2026-08-08)

**Worktree:** `/Users/leestutzman/Documents/GitHub/wowbagger/.claude/worktrees/sleepy-napping-bubble`
**Branch:** `main` (this worktree tracks main directly)
**Status:** clean tree, all work committed and pushed to `origin/main`

> **Next agent:** read this file, then run the queue. It now has a human
> surface: `./bin/wowbagger.js ready --ledger ledger --as-of <today>`.

---

## What shipped 2026-08-08

Eleven ledger items closed, each with TDD, a code review, and a decision
record on the item; two new items filed (34, 35 — 35 closed same day):

- **30** — ADR-0006 records the priority deletion's evidence (deliberate
  scope-narrowing, rationale never written) and the discipline: a contract
  member is never removed in a documentation commit.
- **26** — the `/item/status` create refusal teaches the triage rule; the
  create result already reported `core.status`.
- **29** — the promotion rule is in the mutation contract (item level =
  addressing + payload; `id` is the one dual member); `number` and
  `priority` joined the core view; a test pins the item-level key set.
- **32** — ADR-0007: ownership is a claim concern; no durable field; reopen
  trigger recorded.
- **33** — `wowbagger patch` changes `number`/`priority` under transition's
  lock + CAS via a shared locked-mutation engine (`mutateExistingItem`).
  Mutation contract section 9. The review of item 29's commit found the
  reference oracle rejecting number/priority-bearing core output — both
  oracle sites fixed; create now validates and canonically serializes both
  fields.
- **27** — `wowbagger mint-id` (+ exported `mintId` in `src/mint.js`).
- **28** — human `ready` surface: `#number pri=priority title`.
- **31** — CHANGELOG.md exists; `__proto__` refusal recorded as a behaviour
  change.
- **35** — runner and Claude Code entrypoint import `CORE_COMMAND_ORDER`.
- **18** — claim store failures return contract envelopes: unreadable store
  → `claim-store-unavailable`/`claim-store-unreadable`; failed floor write →
  `clock-floor-persistence-failed`; both exit 6, unchanged.
- **15** — decided: create's default path stays identity-derived; naming
  conventions are Git renames after create; contract states it.

**553 tests**, green on Node 26 and Node 20. README gained an "Upgrading
from an earlier wowbagger" section addressed to agents.

## Authoritative facts (unchanged from 2026-08-07, still binding)

- Test command: `TMPDIR=/tmp node --test test/*.test.js`, on both `node`
  (26) and `/opt/homebrew/opt/node@20/bin/node`.
- Never `git stash` here. `src/` never imports from `test/` or `spec/`.
- `ready --json` is byte-compared by adapter vectors; the human surface is
  separate. Claims are advisory and enforce nothing.
- The version 1 capabilities envelope and adapter core probe do NOT
  advertise `patch` — deliberate; see item 34 and mutation contract §9.

## Standing directive (ratified by Lee, 2026-08-08)

"Work all open items in priority order" means: take the `ready` queue top to
bottom, no re-sorting by size; work each item with full TDD and a review;
raise a blocked item's question to Lee the moment it is reached, park it,
and continue; decompose big items rather than deferring them; and when
context runs low, hand off mid-queue at the exact next step so the following
session resumes without re-negotiation. The program ends when the queue is
empty or holds only items waiting on Lee. Route well-briefed self-contained
coding to Codex headless with a Claude diff review per Lee's routing table.

## The remaining queue, with honest status

Run `./bin/wowbagger.js ready --ledger ledger --as-of <today>` for the live
order. As of this handoff:

| # | Status of the work |
|---:|---|
| 34 | **Gated by design**: folds `patch` into the adapter contract at its next version bump. Do not do this mid-Plans-2/3; the version 1 probe pins operation and command lists exactly. |
| 19 | **Next implementable item.** Needs a contract decision (allow `operation_id` omission when the request was never read — the refuse-before-read ordering is mutation-enforced — versus parsing only `operation_id` early) plus the fixture comparison the spec promised. One focused session. |
| 1 | **Done 2026-08-08.** `adapters/codex/` ships on the shared entrypoint runtime at exact parity with the Claude Code adapter: same wire, same engine, and the runner reports `--target codex` across all fifteen vector cases (79/183 evidenced; the remainder is the shared engine's Plans 2–3, tracked by item 13, and lands for both adapters at once). Closing it required the sanctioned dependent hand-edit: item 11's satisfied `depends_on` entry moved to `related`. |
| 13, 24 | Adapters (Claude Code Plans 2–3, opencode). Multi-session builds. Plan-1 carry-forwards live in `docs/handoffs/2026-08-06-adapter-plan-1.md`. |
| 5, 14 | Contract-authoring items (policy input, consumer configuration). Each wants a dedicated design session and probably Lee's review of the resulting ADR. |
| 7 | Concurrency and crash-recovery test campaign. Self-contained but substantial. |
| 12 | Kimi/OpenAI-compatible integration docs. Blocked in spirit on adapter progress (13/24): documenting integrations that cannot yet run would be fiction. |
| 17 | **Blocked on Lee.** Fenced claims carry an unresolved design question — the transactional coordinator versus the plain-Markdown-and-Git thesis. Full statement in `ledger/2026-08-06-fenced-work-claims-coordinator.md`. Settle before implementing. |
| 22, 23 | **Blocked on external actions.** 22 needs the marketplace publication verified by a real `/plugin marketplace add lstutzman/wowbagger` install (nobody has done one). 23 runs inside PropertyCompass, which prior handoffs exclude from wowbagger sessions without Lee's go-ahead. |
| 2 (triage) | PropertyCompass migration stays unauthorized and gated on fencing (17). |

Items 9, 10, 11, 21 are epics gated on the above and stay out of `ready`.

## Housekeeping (carried forward)

Stale merged branches (`feature/mutation-contract`, `feature/mutation-runtime`,
`feature/read-only-kernel`, `feature/standalone-core-contract`,
`feature/harness-neutral-adapter-contract`, `feature/work-claim-contract`)
remain; deleting them was never authorised — ask first. The main checkout at
`/Users/leestutzman/Documents/GitHub/wowbagger` needs a pull.
