# Handoff — wowbagger (2026-08-10)

**Branch:** `main` · **Worktree:** `/Users/leestutzman/Documents/GitHub/wowbagger`
**State:** clean, everything merged and pushed.

Verify before trusting anything below:

```bash
TMPDIR=/tmp node --test test/*.test.js                                # 634/634
TMPDIR=/tmp /opt/homebrew/opt/node@20/bin/node --test test/*.test.js  # 634/634
TMPDIR=/tmp node spec/run-adapter-implementation.js                   # pass, 15/15, 183/183
node bin/wowbagger.js validate --ledger ledger --json                 # valid
```

## What shipped

**The Claude Code adapter is delivered and item 13 is closed.** All 15 conformance
vectors and 183 assertions pass against the shipped entrypoint on native darwin, on
both runtimes. `docs/adapter-contract.md` §10 records `Implementation-pass (darwin)`
for the eight Claude Code rows. The Codex, Kimi and generic columns stay `Unverified`,
and every platform in every `adapters/*/wowbagger-adapter.json` stays `unverified` —
that is a product-support claim needing per-platform evidence, and it is item 38.

**Schema version 2 is merged and this repository's ledger is migrated.** `depends_on`
now records declared prerequisites rather than only live blockers: a reference to a
`done` item is satisfied history and stays put. Completion copies nothing into
`related`. The done transition is a single-item compare-and-set again, and
`limits.multi_item_atomicity` stays `false`.

Also landed: core and adapter contract version 2, cross-worktree mutation coordination
corrected to `false`, `patch` folded into the adapter contract, the migration tool
(`scripts/migrate-schema-2.js`) with its runbook at `docs/schema-2-migration.md`, and
the shipped skill repointed at contract 2.

## The thing worth knowing

**A green conformance number on this runner has been wrong three times.** Each time it
reported `183/183 pass` while measuring nothing real:

1. It never touched the shipped entrypoint. Proved by deleting the trailing LF from the
   bootstrap response and watching conformance stay green while the real wire tests
   failed 18 of 20.
2. It launched real core children for the `negative-capability` cases, whose entire
   obligation is that no launch occurs.
3. It enforced no-launch at the injection point only, so a child spawned as an ignored
   side effect was invisible.

Every one was caught by an adversarial pass that assumed the previous conclusion was
wrong. None by the implementation work that produced it.

**So do not trust the number. Trust that it can fail.** Before believing any conformance
result, break something and confirm the run goes red:

| Mutation | Expected |
|---|---|
| Insert an awaited real core child launch as an ignored side effect | 4 negative-capability cases red |
| Clamp the child-output byte counter so overflow is never detected | `bounded-output` red |
| Remove the trailing LF from `writeBootstrapResponse` | runner rejects the wire |
| Remove one byte from forwarded core stdout | 3 equivalence cases red |
| Reverse declared protocol instruction order | `instruction-input` red |

## Open work

| Item | Status | What |
|---|---|---|
| 37 | triage | Adapter behaviour under concurrent invokes — never exercised. Item 7 covered core mutation concurrency, a different boundary. |
| 38 | triage | Earn a `supported` platform claim on linux and win32. Case 12 carries drive, UNC, device and volume refusals no darwin run exercises for real. |
| 39 | triage | The bootstrap wire's duplicated refusal paths, and `describe` carrying no byte ceiling. |
| 41 | triage | Confirmed adapter defects: a launcher exception fabricating a definite `started:false` for a mutation that may have applied; envelope validation covering only three core commands; unbound response contents; type coercion in the approval nonce and instruction source ID. |
| 36 | backlog | `--help` and `--version` surfaces. |
| 11, 22, 23 | backlog | Packaging, marketplace publication, PropertyCompass dogfood — all unblocked now item 13 is done. |
| 17 | backlog | Fenced work claims. Claims stay advisory until then; nothing may advertise otherwise. |

**Item 41 is the sharpest of these.** A mutation that already applied being reported as
`core-launch-failed` invites a caller to retry it.

## Standing rules

- **`TMPDIR=/tmp` on every test command.** Without it the lock socket path exceeds the
  macOS limit and runs fail for an environmental reason. `npm run check` does not set it
  and therefore fails by default — a known one-line defect, unfiled.
- **Never `git stash` here.** Several worktrees share one stash stack.
- **`spec/adapter-reference.js` is an independent oracle.** Never modify it; never import
  it into `src/`. Importing it makes the vectors tautological.
- **`src/` imports nothing from `test/` or `spec/`.**
- **The fixtures are normative.** Where contract prose and a fixture disagree, the
  fixture wins. Changing a vector to match an implementation is how a defect becomes a
  passing number — if a fixture fails and no contract clause makes it wrong, the code is
  wrong.
- **Coverage is judged by mutation**, not counts. Break each guard, confirm a test goes
  red. This repository has shipped inert guards more than once.
- Node 20 lives at `/opt/homebrew/opt/node@20/bin/node`, not on PATH.

## Ledger friction found by dogfooding

Three gaps surfaced while using wowbagger on itself. None is filed; all are real.

- **Relations cannot be edited.** `patch` accepts exactly `number` and `priority`. Items
  37, 38 and 39 were filed with `depends_on` where `related` was correct, and there is no
  supported way to fix them.
- **A change of direction cannot be recorded.** Decisions attach only to edges that
  append evidence, and `backlog → in-progress` appends none. Item 44's reversal had to
  live in a design document instead.
- **`create` accepts an item with no `number`**, producing a handle-less item despite
  item 28 having established that every item gets one.

## Reference

- `docs/design/2026-08-09-multi-item-atomicity.md` — the rejected multi-item design, the
  adversarial review that killed it, and the recommendation that replaced it. Read this
  before revisiting dependency semantics.
- `docs/schema-2-migration.md` — migration runbook.
- `docs/adapter-contract.md` §10 — conformance, the mode-dispatch rule, status table.
- `docs/handoffs/2026-08-09-adapter-completion.md` — corrected; carries a notice about
  the fabricated version it replaced.
