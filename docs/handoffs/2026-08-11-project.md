# Handoff — wowbagger (2026-08-11)

**Branch:** `main` · **Worktree:** `/Users/leestutzman/Documents/GitHub/wowbagger`
**State:** clean, everything committed and pushed to `origin/main`.

Verify before trusting anything below:

```bash
TMPDIR=/tmp node --test test/*.test.js                                # 657/657
TMPDIR=/tmp /opt/homebrew/opt/node@20/bin/node --test test/*.test.js  # 657/657
TMPDIR=/tmp node spec/run-adapter-implementation.js                   # pass
node bin/wowbagger.js validate --ledger ledger --json                 # valid
```

This handoff supersedes `2026-08-10-project.md`. Read that one too for the
standing oracle/atomicity/coverage context it records; this one records what
moved this session and where the queue now stands.

## What shipped this session

- **Item 36 — done.** `wowbagger --help` (command inventory), per-command
  `--help`, `--version`, and a "Did you mean …?" typo suggestion on unknown
  commands, with outcome-0 and aliases `-h`/`-v`. Machine JSON surfaces,
  exit codes, and genuine usage-error refusals unchanged. 10 new
  mutation-guarded tests; README core-commands section points at `--help`.
  Commits `738179c` (fix) and the ledger close.
- **Item 11 — done.** Packaged wowbagger for public npm distribution under the
  plain `wowbagger` name (decision: Option B). `package.json` is public with a
  `files` allowlist (bin, src, skills, adapters + docs/license), `bin` map,
  prepublishOnly guard that validates the ledger and syntax-checks the binary.
  Verified honestly: the tarball installs cleanly in an empty dir and
  `--version`/`--help`/`validate` run against the real ledger. README gained
  an Installation/Compatibility/Security section. **The first `npm publish`
  is deliberately NOT run** — it needs your credentials and is the actual
  release step. `0.1.0-prealpha` remains the version; decide a clean tag at
  publish time. Commit `c16c437`.
- **Item 22 — deferred (not closed).** Marketplace publication parked. Reason:
  hold until the other issues are finished and dogfooding proves the tool.
  The ledger has **no `deferred` status**, so the park was recorded in
  `2026-08-10-project.md` rather than in-band (this is the "change of
  direction"/"deferral" ledger friction — see below).
- **Item 46 — new backlog item filed.** "Allow an item to be marked
  deferred/held after creation" — the gap that blocked recording #22's deferral
  in-band. Minted, created, accepted to backlog, given handle 46 via `patch`
  (which re-hit the known "create accepts an item with no `number`" friction —
  the item was created handle-less and I had to patch 46 on). Commit
  `be25b20`.
- **Item 17 — design written and decisions recorded, no code.** Fenced work
  claims under Option 2: a merge-coordinated git journal
  (`mode: "merge-coordinated"`, `safe_exclusive_dispatch: false`, cooperative
  writers only). Full design + adversarial review + decision in
  `docs/design/2026-08-10-fenced-work-claims-merge-coordinated.md`, produced by
  two independent subagents (designer + attacker). Three implementation
  parameters settled: additive enum values, 8 MiB / 65,536-entry limits per
  namespace, per-namespace reconcile log. Commits `14c2e43` (design),
  `1b1d9f9` (open-item decisions). Not yet coded.
- **Item 39 — done.** Closed the bootstrap wire's duplicated refusal paths,
  three contract decisions:
  - **D1** — bound `describe` reads to `max_request_bytes` (65536); was
    unbounded (the first call any host makes). The observable property is the
    bounded reader bails on an oversized never-ending stream instead of
    hanging (mutation: removing the ceiling makes that test hang).
  - **D2** — refusal messages moved to a single `src/adapter/messages.js`
    source (`INVOKE_MESSAGES`) consumed by `invoke.js` and the entrypoint;
    wire-level test pins the message so it can't silently diverge from the
    reference model. **The reference oracle was NOT touched.**
  - **D3** — unknown operations are refused as `invalid-invocation` BEFORE
    reading stdin (previously they read stdin, misreported malformed reads, and
    hung on a host that never closed stdin; mutation proves the guard).
  - Also fixed the `readBootstrapRequest` parse-branch detail asymmetry.
  Suite 657/657 both runtimes, adapter conformance pass, ledger valid.
  Commit `9a91a64`.

## The thing worth knowing (unchanged, still true)

**A green conformance number on this runner has been wrong multiple times.** Do
not trust the number; trust that it can fail. Before believing any conformance
result, break something and confirm the run goes red. Item 39's D1 fix is the
latest lesson: a *weak* test passed unguarded (oversized-but-invalid JSON
refused either way); the honest test asserts the hang-prevention property that
only the real guard provides.

## Open work

| Item | Status | What |
|---|---|---|
| 36 | done | `--help`, `--version`, typo suggestion. |
| 11 | done | Public npm packaging + install/compat/security docs. **First `npm publish` is your next release step.** |
| 39 | done | Bootstrap-wire refusal-path dedup, bounded describe, refuse unknown ops pre-stdin. |
| 46 | backlog | Deferral/on-hold gap (filed this session; gate for recording any future in-band park). |
| 17 | backlog | **Design + decisions recorded, implementation deferred.** See `docs/design/2026-08-10-fenced-work-claims-merge-coordinated.md`. |
| 37 | backlog | Validate the adapter under concurrent invokes. |
| 38 | backlog | Earn a `supported` platform claim on linux/win32 (needs per-platform release evidence). |
| 23 | backlog | PropertyCompass dogfood from the installed skill (after the above). |
| 22 | backlog | **Deferred.** Marketplace publication (waiting on the other issues + dogfood proof). |

Ready order (by `ready --as-of 2026-08-11`, advisory): #46 → #10 → #17 → #22 → #23 → #37
→ #38. Treating #22 as deferred and #17 as designed-but-not-coded, the actionable
next is **#37 (adapter under concurrent invokes)**, then **#38**, then **#23**.
(Item 10, "prepare a versioned standalone release", also sits ready and pairs
with item 11's packaging — it is the version/tag/release step that follows the
first `npm publish` decision.)

## Standing rules

- **`TMPDIR=/tmp` on every test command.** Without it the lock socket path
  exceeds the macOS limit. `npm run check` does not set it and fails by
  default (known one-line defect, unfiled).
- **Never `git stash` here.** Worktrees share one stash stack.
- **`spec/adapter-reference.js` is an independent oracle.** Never import it
  into `src/`; never modify it to make a vector agree with a buggy
  implementation. The one authorized exception (a *shared* confirmed defect,
  fixed in both and pinned) is recorded in `2026-08-10-project.md`.
- **`src/` imports nothing from `test/` or `spec/`.**
- **The fixtures are normative.** If a fixture fails and no contract clause
  makes it wrong, the code is wrong.
- **Coverage is judged by mutation**, not counts. Break each guard; confirm a
  test goes red (or hangs, which is also evidence — e.g. item 39's D1/D3).
- Node 20 lives at `/opt/homebrew/opt/node@20/bin/node`, not on PATH.
- `.claude/` and `.pi-subagents/` are now gitignored (local tool state);
  keep them out of commits.
- Every shipped change should first pass the four-command verification block
  at the top of this handoff.

## Ledger friction found by dogfooding (repeated here)

- **An item cannot be marked deferred/held.** No `deferred` status, no defer
  edge; `snoozed_until` is create-only. Item 22's deferral had to live in the
  handoff. **Filed as item 46.**
- **Relations cannot be edited.** `patch` accepts exactly `number` and
  `priority`; items filed with `depends_on` where `related` was correct cannot
  be fixed in-band.
- **A change of direction cannot be recorded.** Decisions attach only to
  evidence-appending edges.
- **`create` accepts an item with no `number`**, producing a handle-less item
  (re-hit when filing item 46; item 46's handle had to be `patch`ed on).

## Reference

- `docs/design/2026-08-10-fenced-work-claims-merge-coordinated.md` — item 17
  design + adversarial review + decisions. Read before implementing fencing.
- `docs/design/2026-08-09-multi-item-atomicity.md` — the rejected multi-item
  design; its review's findings are a standing hazard for any coordinator work.
- `docs/handoffs/2026-08-10-project.md` — prior session: item 41 oracle
  decision, the legacy "don't trust the green number" evidence, ledger
  friction origin.
- `docs/adapter-contract.md` §3.3 / §10 — bootstrap wire and conformance.
