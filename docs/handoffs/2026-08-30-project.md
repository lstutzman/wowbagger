# Project handoff — 2026-08-30

## Current state

This handoff resumes from the `main-2` worktree before integration into `main`.

- Source branch: `main-2`
- Source commit: `3d45e6cfd1226892e2555ac02e1880ced36900e2`
- `origin/main-2`: `3d45e6cfd1226892e2555ac02e1880ced36900e2`
- `origin/main` before integration: `863fceed0840184a3bdecc7beba3fdef92071f85`
- npm dist-tags: `latest` and `next` both point to `0.1.0-alpha.17`
- Working ledger validation: `valid: true`, `errors: []`
- Working tree was clean before this handoff document was created.

The next session must integrate the handoff commit into local `main`, then push
`origin/main`. Do not force-push or rewrite either branch.

## Completed work

### #182 — duplicate immutable number recovery

Shipped in `0.1.0-alpha.15`.

- Added the separate `ledger-repair` contract version 1.
- Added read-only `number-repair-proposal`.
- Added explicit multi-item `number-repair` mappings with snapshot, revision,
  old-number, collision, and successor-ledger validation.
- Added durable no-follow candidate staging, intent/final journal entries,
  shared namespace fencing, atomic publication, interrupted recovery, and
  bounded auto-commit recovery tokens.
- Corrected response-domain and host-schema inventories.
- Item is closed in the ledger.

### #185 — skill/core version drift

Shipped in `0.1.0-alpha.16`.

- Added `version-drift --json`, with optional `--skill <path>`.
- Compares installed skill distribution and contract pins with the required
  package and running core values.
- Reports provenance as `registry-package`, `git-tag`, `plugin-cache`,
  `global-link`, `direct-path`, or `unknown` when detectable.
- Added stale-skill and incompatible-newer-skill tests.
- Item is closed in the ledger.

### #188 — Node 24 runtime floor

Shipped in `0.1.0-alpha.17`.

- Raised `engines.node` and `MINIMUM_NODE_MAJOR` from 20 to 24.
- Updated Linux, Windows, and macOS CI to Node 24.
- Added `.nvmrc` with `24`.
- Release tooling now invokes `/opt/homebrew/opt/node@24/bin/node` and runs
  the strict-deprecation gate.
- Updated runtime documentation, ADR 0002, host contract, skill, README,
  adapter release path, changelog, and current project instructions.
- Node 26 remains excluded because of Lee's separate stated Vitest constraint.
- Normalized Git worktree paths per platform.
- Skipped only Windows fixtures that require POSIX mode bits, newline path
  components, or Git roster shims unavailable on Windows.
- Item is closed in the ledger.

## Verification evidence

The alpha.17 release gate passed:

- full Node 24 suite;
- strict deprecation suite with `--pending-deprecation --throw-deprecation`;
- adapter implementation vectors for Claude Code, Codex, and OpenCode;
- ledger validation;
- production dependency audit;
- worktree and index whitespace checks.

GitHub Actions workflow run `33323537756` passed on commit
`3d45e6cfd1226892e2555ac02e1880ced36900e2` for Linux, macOS, and Windows
Node 24 jobs. The workflow emitted GitHub's warning that `actions/checkout@v4`
and `actions/setup-node@v4` internally target Node 20; that warning concerns
GitHub action runtimes, not the repository's supported Node floor.

Local verification commands:

```sh
TMPDIR=/tmp /opt/homebrew/opt/node@24/bin/node --test test/*.test.js
TMPDIR=/tmp /opt/homebrew/opt/node@24/bin/node --pending-deprecation --throw-deprecation --test test/*.test.js
TMPDIR=/tmp /opt/homebrew/opt/node@24/bin/node spec/run-adapter-implementation.js
TMPDIR=/tmp /opt/homebrew/opt/node@24/bin/node bin/wowbagger.js validate --ledger ledger --json
/opt/homebrew/opt/node@24/bin/npm audit --omit=dev
```

The machine-wide `wowbagger` command still resolves to the older global
`0.1.0-alpha.14` installation through NVM. Use the explicit repository binary
and Node 24 path for this checkout, or update the global installation in a
separate deliberate operation. `version-drift --json` is intended to expose
that mismatch rather than hide it.

## Remaining open ledger items

The current open-item order is:

1. **#183 — P2 — backlog:** Make standard tags safely mutable on existing
   ledgers.
2. **#184 — P2 — backlog:** Scope claim verification away from unrelated active
   work.
3. **#187 — P2 — backlog:** Align journal capacity refusal reason with contract.
4. **#142 — P3 — in progress:** Submit Wowbagger to the Claude plugin directory;
   awaiting Anthropic review or returned findings.
5. **#174 — P3 — backlog:** Recognize parent-migrate and snooze lock owners in
   lock diagnostics.
6. **#186 — P3 — backlog:** Design safe batch create allocation and publication.

#183 is the next implementation target. Its first design slice compares
standard patchable `tags` with a declaration-bootstrap path. Do not implement
before the design approval gate. Use TDD and preserve existing YAML values.

## Operating constraints

- Work one ledger issue at a time in priority order.
- The orchestrator owns lifecycle transitions, Git integration, shared CI, and
  external publication actions.
- Use the ledger's ubiquitous terms exactly: item, ledger, work claim,
  reconciliation, namespace, and repair.
- Never use `git stash`.
- Use `TMPDIR=/tmp` for test commands.
- Treat `spec/adapter-reference.js` and `test/work-claim-reference.js` as
  independent oracles. Do not import or modify them to fit implementation.
- Preserve the hard runtime floor at Node 24. Do not add Node 26 to supported
  gates until Lee clears the external Vitest constraint.
- Run the full relevant verification gate before closing an item.
