---
schema_version: 2
id: wb_01M086JNH36RR56051D89Y3ANQ
number: 121
title: "Make publish-claimed reconcile unconditionally like the legacy fence"
kind: task
priority: 10
status: backlog
created: 2026-08-17
updated: 2026-08-17
provenance:
  source: "maintainer-dogfood"
  recorded_at: "2026-08-17T15:48:53Z"
depends_on: []
related: []
decisions:
  - action: accept
    date: 2026-08-17
    summary: "Accept into the backlog."
    rationale: "Verified guard shape at src/claim-publication.js:177; contract prose says any unreconciled prior mutation refuses. Repro-first."
---

Verified contract gap, found during the 2026-08-17 ideation enrichment (docs/ideation/enrichments/2026-08-17-autocommit.md, hidden risk 8): `publishClaimed` runs journal reconciliation only when it observes an unresolved publish-intent (src/claim-publication.js:177-181 - the guard is `entry.type === 'publish-intent' && !terminalKeys.has(...)`), while the work-claim contract states that ANY unreconciled prior mutation must refuse the next mutating command, publish-claimed included (docs/work-claim-contract.md, the commit-per-mutation invariant section). A legacy transition left uncommitted therefore refuses the next legacy mutation (the fence reconciles unconditionally) but does NOT refuse the next publish-claimed unless a pending intent happens to exist.

Scope:
1. Reproduce in a fixture: provisioned ledger, committed baseline, legacy transition left uncommitted, then a publish-claimed on another item - pin what actually happens today (the enrichment's reading says it proceeds; do not trust, run it).
2. If confirmed: publishClaimed reconciles unconditionally before the fence decision, matching the legacy path and the contract prose; the refusal shape is the existing exit 6 claim-store-unavailable / publication-reconciliation-required with its findings.
3. If the current behavior turns out to be deliberate (publish-claimed validates its own item's revision at HEAD through a different mechanism that makes the global reconcile redundant), document that mechanism in the contract instead and record why the prose stands.

Acceptance:
- A fixture pins the chosen behavior; contract prose and code agree; mutation-guarded.
- Gate green on both runtimes.
